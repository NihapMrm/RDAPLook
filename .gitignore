/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
# .env.production is intentionally tracked — it contains only placeholders, no real secrets.
# Real secrets (APP_KEY, RAPIDAPI_PROXY_SECRET) are set directly in .env on the server.
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
/.claude